import { FamilyTreeService } from './family-tree-service';

describe('FamilyTreeService', () => {
  it('should create an instance', () => {
    expect(new FamilyTreeService()).toBeTruthy();
  });
});
